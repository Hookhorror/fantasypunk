using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DwarfBoss : Enemy
{
    // Start is called before the first frame update
    void Start()
    {
        maxHealth = 69;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
